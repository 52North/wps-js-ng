import { CapabilitiesResponse } from './capabilities-response';

describe('CapabilitiesResponse', () => {
  it('should create an instance', () => {
    expect(new CapabilitiesResponse(null)).toBeTruthy();
  });
});
