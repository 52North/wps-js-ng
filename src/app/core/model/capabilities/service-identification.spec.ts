import { ServiceIdentification } from './service-identification';

describe('ServiceIdentification', () => {
  it('should create an instance', () => {
    expect(new ServiceIdentification(null)).toBeTruthy();
  });
});
