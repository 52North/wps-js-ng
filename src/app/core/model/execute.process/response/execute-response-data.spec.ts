import { ExecuteResponseData } from './execute-response-data';

describe('ExecuteResponseData', () => {
  it('should create an instance', () => {
    expect(new ExecuteResponseData(undefined)).toBeTruthy();
  });
});
